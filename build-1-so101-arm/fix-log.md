# Fix log

| Date | Problem | My guess (written before the fix) | The one change I made | Before (out of 20) | After (out of 20) | Was my guess right? |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-18 | TorchCodec would not load after the LeRobot install. Error: "Could not load libtorchcodec", missing libavutil.60.dylib. `conda install ffmpeg -c conda-forge` had installed ffmpeg 9.0.1, and TorchCodec 0.11.1 supports ffmpeg 4 to 8 only. | No guess given. Tony said "lets fix it". | Pinned ffmpeg in the env: `conda install ffmpeg=7.1.1 -c conda-forge` (the fallback in the official LeRobot install guide). | n/a (install, not a trial test) | n/a. After the change `import torchcodec` works and libsvtav1 encoder is present. | n/a |
| 2026-09-21 | Wrist_Roll_Pitch and Rotation_Pitch came off the bed after a few layers, twice. Spaghetti. Other parts on the same plate printed fine. Plate was not washed. | "I will print without changing the orientation, so it prints the dev has given to print" | Added a brim to only these two parts (per-object setting: outer brim only, 5 mm). Round parts have a tiny bed contact (about 30 mm2 and 330 mm2 measured from the STL). | n/a (print, not a trial test) | Both parts stuck and printed clean (23 Sep). | Wrong. Orientation was already the dev's and it still failed. Brim was the fix. |
| 2026-09-23 | Brim would not separate from the parts. Fused to the edge. | No guess given. | Two changes, logged together: inner+outer brim changed to outer brim only, and brim-object gap 0 to 0.1 mm. | n/a | Peels off, still needs a knife at the edge. Better than before. Next try 0.15 mm gap. | n/a |
